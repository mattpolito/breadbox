#main_content
  %h2 New invoice
  - form_for(@invoice) do |f|
    = render :partial => 'form', :locals => { :f => f, :button_text => 'Create' }
  
#sidebar
  = link_to 'Cancel &amp; Go Back', invoices_path, :id => 'cancel'