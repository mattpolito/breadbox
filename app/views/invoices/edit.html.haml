#main_content
  %h2 Editing invoice
  - form_for(@invoice) do |f|
    = render :partial => 'form', :locals => { :f => f, :button_text => 'Update' }

#sidebar
  = link_to 'Cancel', invoices_path, :id => 'cancel'