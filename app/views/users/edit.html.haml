#main_content
  %h2= "Edit #{@user.name}"
  - form_for @user do |f|
    = render :partial => "form", :locals => { :f => f, :button_text => 'Update' }

#sidebar
  = link_to "Cancel &amp; Go Back", root_url, :class => "button", :id => "cancel"